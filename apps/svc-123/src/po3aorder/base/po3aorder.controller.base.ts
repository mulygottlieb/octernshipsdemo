/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { Po3aorderService } from "../po3aorder.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { Po3aorderCreateInput } from "./Po3aorderCreateInput";
import { Po3aorderWhereInput } from "./Po3aorderWhereInput";
import { Po3aorderWhereUniqueInput } from "./Po3aorderWhereUniqueInput";
import { Po3aorderFindManyArgs } from "./Po3aorderFindManyArgs";
import { Po3aorderUpdateInput } from "./Po3aorderUpdateInput";
import { Po3aorder } from "./Po3aorder";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class Po3aorderControllerBase {
  constructor(
    protected readonly service: Po3aorderService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Po3aorder })
  @nestAccessControl.UseRoles({
    resource: "Po3aorder",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(@common.Body() data: Po3aorderCreateInput): Promise<Po3aorder> {
    return await this.service.create({
      data: {
        ...data,

        of_store: data.of_store
          ? {
              connect: data.of_store,
            }
          : undefined,
      },
      select: {
        createdAt: true,
        id: true,

        of_store: {
          select: {
            id: true,
          },
        },

        t1: true,
        t2: true,
        t3: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [Po3aorder] })
  @ApiNestedQuery(Po3aorderFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Po3aorder",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<Po3aorder[]> {
    const args = plainToClass(Po3aorderFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        createdAt: true,
        id: true,

        of_store: {
          select: {
            id: true,
          },
        },

        t1: true,
        t2: true,
        t3: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Po3aorder })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Po3aorder",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: Po3aorderWhereUniqueInput
  ): Promise<Po3aorder | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,
        id: true,

        of_store: {
          select: {
            id: true,
          },
        },

        t1: true,
        t2: true,
        t3: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Po3aorder })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Po3aorder",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: Po3aorderWhereUniqueInput,
    @common.Body() data: Po3aorderUpdateInput
  ): Promise<Po3aorder | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          of_store: data.of_store
            ? {
                connect: data.of_store,
              }
            : undefined,
        },
        select: {
          createdAt: true,
          id: true,

          of_store: {
            select: {
              id: true,
            },
          },

          t1: true,
          t2: true,
          t3: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Po3aorder })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Po3aorder",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: Po3aorderWhereUniqueInput
  ): Promise<Po3aorder | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,
          id: true,

          of_store: {
            select: {
              id: true,
            },
          },

          t1: true,
          t2: true,
          t3: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
